# i = 1
# while i <= 5:
#     print('*' * i)
#     i += 1

"""
在默认情况下，`print` 函数输出内容之后，会自动在内容末尾增加换行
如果不希望末尾增加换行，可以在 `print` 函数输出内容的后面增加 `, end=""`
其中 `""` 中间可以指定 `print` 函数输出内容之后，继续希望显示的内容
"""

i = 1
j = 1
while i <= 5:
    j = i
    while j > 0:
        print('*', end='')
        j -= 1
    # 这行代码的目的就是在一行星星输出完成后换行
    print('')
    i += 1
